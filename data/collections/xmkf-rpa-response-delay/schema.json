{
  "workflowFields": [
    {
      "name": "responseDelayConfig",
      "type": "Json?",
      "model": "Workflow",
      "desc": "终态：enabled/delaySeconds/随机区间；中间态：waitingEnabled/waitingDelaySeconds（可独立于 enabled）"
    }
  ],
  "runFields": [
    {
      "name": "responseDelaySeconds",
      "type": "Int?",
      "model": "WorkflowRun",
      "desc": "本次 run 终态生效延迟秒数；null=不延迟"
    },
    {
      "name": "resultScheduledAt",
      "type": "DateTime?",
      "model": "WorkflowRun",
      "desc": "终态计划对外投递时间（finishRun 写入）"
    },
    {
      "name": "resultDeliveredAt",
      "type": "DateTime?",
      "model": "WorkflowRun",
      "desc": "终态实际投递时间；null=尚未对外返回"
    },
    {
      "name": "input.__waitingNotify",
      "type": "Json（运行时）",
      "model": "WorkflowRun.input",
      "desc": "中间态：{ scheduledAtMs, delaySeconds, signalName, deliveredAtMs? }；无新 Prisma 列"
    }
  ],
  "configExample": {
    "enabled": true,
    "delaySeconds": 30,
    "delaySecondsMin": 10,
    "delaySecondsMax": 60,
    "waitingEnabled": true,
    "waitingDelaySeconds": 15
  },
  "requestOverride": {
    "field": "responseDelaySeconds",
    "rules": [
      "仅覆盖终态延迟",
      "0 = 强制关闭终态延迟",
      ">0 = 覆盖工作流终态配置",
      "省略 = 读工作流 responseDelayConfig.enabled",
      "中间态只认 waitingEnabled，不走此覆盖"
    ]
  }
}
